# 题目描述

这是LeetCode上的[207.课程表](https://leetcode.cn/problems/course-schedule/)。

难度：**中等**

Tag：深度优先搜索、广度优先搜索、图、拓扑排序

你这个学期必须选修`numCourses`门课程，记为`0`到`numCourses - 1`。

在选修某些课程之前需要一些先修课程。先修课程按数组`prerequisites`给出，其中 $prerequisites[i] = [a_i, b_i]$，表示如果要学习课程 $a_i$则**必须**先学习课程 $b_i$。

- 例如，先修课程对`[0, 1]`表示：想要学习课程`0`，你需要先完成课程`1`。

请你判断是否可能完成所有课程的学习？如果可以，返回`true`；否则，返回`false`。

示例1：

```
输入：numCourses = 2, prerequisites = [[1,0]]
输出：true
解释：总共有 2 门课程。学习课程 1 之前，你需要完成课程 0 。这是可能的。
```


示例2：

```
输入：numCourses = 2, prerequisites = [[1,0],[0,1]]
输出：false
解释：总共有 2 门课程。学习课程 1 之前，你需要先完成课程 0 ；并且学习课程 0 之前，你还应先完成课程 1 。这是不可能的。
```

提示：

- `1 <= numCourses <= 2000`
- `0 <= prerequisites.length <= 5000`
- `prerequisites[i].length == 2`
- $0 <= a_i, b_i < numCourses$
- `prerequisites[i]`中的所有课程对**互不相同**

# 解题思路

- **本题可约化为**：课程安排图是否是**有向无环图(DAG)**。即课程间规定了前置条件，但不能构成任何环路，否则课程前置条件将不成立。
- 思路是通过**拓扑排序**判断此课程安排图是否是**有向无环图(DAG)**。
- **拓扑排序原理**：对DAG的顶点进行排序，使得对每一条有向边`(u,v)`，均有`u`（在排序记录中）比`v`先出现。亦可理解为对某点v而言，只有当`v`的所有源点均出现了，`v`才能出现。
- 通过课程前置条件列表`prerequisites`可以得到课程安排图的**邻接表**`adjacency`，以降低算法时间复杂度，以下两种方法都会用到邻接表。

## 方法一：入度表（广度优先遍历）

### 算法流程

**初始化变量**：

- 统计课程安排图中每个节点的入度，生成**入度表**`indegrees`。
- 生成有向图，索引是先修课程，值是后修课程。

**初始化队列**：借助一个队列`queue`，将所有入度为0的节点入队。

**当`queue`非空时，循环迭代**：

- 依次将队首节点出队，将`pre`放入`studied`列表，表示已学习。

- 获取`pre`课程对应的所有后修课程列表后遍历，当前后修课程为`cur`：
  - 后修课程`cur`的入度自减1。
  - 若当前后修课程`cur`的入度为0，则加入队列`

### 例子

**有向无环图**：

```
   c1  c2
  /  \ / \
  ↓   ↓   ↓
 c8  c3  c5
  |    \ /
  ↓     ↓
 c9    c4
   \  /  \
    \/   ↓
    c7   c6

初始化:
    入度表:
        c1 c2 c3 c4 c5 c6 c7 c8 c9
        0  0  2  2  1  1  2  1  1
    邻接表:
          c1  |   c2  | c3 |   c4  | c5 | c6 | c7 | c8 | c9
        c3,c8 | c3,c5 | c4 | c6,c7 | c4 | [] | [] | c9 | c7

初始化队列:
    queue: front[c1,c2]back
    入度表更新后:
        c3 c4 c5 c6 c7 c8 c9
        2  2  1  1  2  1  1

第一次迭代:
    c1出队后，放入studied列表。
        queue: front[c2]back
        studied: [c1]
    c3和c8的入度减1:
        c3 c4 c5 c6 c7 c8 c9
        1  2  1  1  2  0  1
    c8入度为0，入队:
        queue: front[c2,c8]back
        入度表:
            c3 c4 c5 c6 c7 c9
            1  2  1  1  2  1

第二次迭代:
    c2出队后，放入studied列表。
        queue: front[c8]back
        studied: [c1,c2]
    c3和c5的入度减1:
        c3 c4 c5 c6 c7 c9
        0  2  0  1  2  1
    c3和c5入度为0，入队:
        queue: front[c8,c3,c5]back
        入度表:
            c4 c6 c7 c9
            2  1  2  1

第三次迭代:
    c8出队后，放入studied列表。
        queue: front[c3,c5]back
        studied: [c1,c2,c8]
    c9的入度减1:
        c4 c6 c7 c9
        2  1  2  0
    c9入度为0，入队:
        queue: front[c3,c5,c9]back
        入度表:
            c4 c6 c7
            2  1  2

第四次迭代:
    c3出队后，放入studied列表。
        queue: front[c5,c9]back
        studied: [c1,c2,c8,c3]
    c4的入度减1:
        c4 c6 c7
        1  1  2 
    无入度为0，不更新queue

第五次迭代:
    c5出队后，放入studied列表。
        queue: front[c9]back
        studied: [c1,c2,c8,c3,c5]
    c4的入度减1:
        c4 c6 c7
        0  1  2
    c4入度为0，入队:
        queue: front[c9,c4]back
        入度表:
            c6 c7
            1  2

第五次迭代：
    c9出队后，放入studied列表。
        queue: front[c4]back
        studied: [c1,c2,c8,c3,c5,c9]
    c7的入度减1:
        c6 c7
        1  1
    无入度为0，不更新queue

第六次迭代：
    c4出队后，放入studied列表。
        queue: front[]back
        studied: [c1,c2,c8,c3,c5,c9,c4]
    c6和c7的入度减1:
        c6 c7
        0  0
    c6和c7的入度为0，入队:
        queue: front[c6,c7]back
        入度表: []

第七次迭代：
    c6出队后，放入studied列表。
        queue: front[c7]back
        studied: [c1,c2,c8,c3,c5,c9,c6]
    c6无后修课程，结束当前迭代。

第八次迭代：
    c7出队后，放入studied列表。
        queue: front[]back
        studied: [c1,c2,c8,c3,c5,c9,c6,c7]
    c7无后修课程，结束当前迭代。

队列为空，结束迭代。

Studied列表的元素个数 == 课程数量，是无环图。
```

有向有环图：

```
有向图:
   c1  c2
  /  \ / \
  ↓   ↓   ↓
 c8  c3  c5←+
  |    \ /  |
  ↓     ↓   |
 c9    c4   |
   \  /  \  |
    \/   ↓  |
    c7   c6-+

初始化:
    入度表:
        c1 c2 c3 c4 c5 c6 c7 c8 c9
        0  0  2  2  2  1  2  1  1
    邻接表:
          c1  |   c2  | c3 |   c4  | c5 | c6 | c7 | c8 | c9
        c3,c8 | c3,c5 | c4 | c6,c7 | c4 | c5 | [] | c9 | c7

初始化队列:
    queue: front[c1,c2]back
    入度表更新后:
        c3 c4 c5 c6 c7 c8 c9
        2  2  2  1  2  1  1

第一次迭代:
    c1出队后，放入studied列表。
        queue: front[c2]back
        studied: [c1]
    c3和c8的入度减1:
        c3 c4 c5 c6 c7 c8 c9
        1  2  2  1  2  0  1
    c8入度为0，入队:
        queue: front[c2,c8]back
        入度表:
            c3 c4 c5 c6 c7 c9
            1  2  2  1  2  1

第二次迭代:
    c2出队后，放入studied列表。
        queue: front[c8]back
        studied: [c1,c2]
    c3和c5的入度减1:
        c3 c4 c5 c6 c7 c9
        0  2  1  1  2  1
    c3入度为0，入队:
        queue: front[c8,c3]back
        入度表:
            c4 c5 c6 c7 c9
            2  1  1  2  1

第三次迭代:
    c8出队后，放入studied列表。
        queue: front[c3]back
        studied: [c1,c2,c8]
    c9的入度减1:
        c4 c5 c6 c7 c9
        2  1  1  2  0
    c9入度为0，入队:
        queue: front[c3,c9]back
        入度表:
            c4 c5 c6 c7
            2  1  1  2

第四次迭代:
    c3出队后，放入studied列表。
        queue: front[c9]back
        studied: [c1,c2,c8,c3]
    c4的入度减1:
        c4 c5 c6 c7
        1  1  1  2
    无入度为0，不更新queue

第五次迭代:
    c9出队后，放入studied列表。
        queue: front[]back
        studied: [c1,c2,c8,c3,c9]
    c7的入度减1:
        c4 c5 c6 c7
        1  1  1  1
    无入度为0，不更新queue

队列为空，结束迭代。
Studied列表的元素个数 != 课程数量，是无环图。
```

### 代码

```c++
class Solution {
public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        // 存储每个节点的入度
        vector<int> indegrees(numCourses, 0);
        // 存储有向图, 索引是先修课程，值是后修课程
        vector<vector<int>>  (numCourses);

        // 统计入度和构建邻接表
        for (const auto& cp : prerequisites) {
            indegrees[cp[0]]++;                 // 入度统计
            adjacency[cp[1]].push_back(cp[0]);  // 有向图填充
        }

        // 将所有入度为 0 的课程入队
        queue<int> que;
        for (int i = 0; i < numCourses; ++i) {
            if (indegrees[i] == 0) {
                que.push(i);
            }
        }

        // 学习的课程的顺序
        vector<int> studied;

        // BFS 拓扑排序
        while (!que.empty()) {
            int pre = que.front();
            que.pop();
            studied.push_back(pre);

            for (int cur : adjacency[pre]) {
                if (--indegrees[cur] == 0) {
                    que.push(cur);
                }
            }
        }
        return numCourses == studied.size();
    }
};
```

### 复杂度分析

- **时间复杂度**$O(n + m)$：其中`n`为课程数，`m`为先修课程的要求数。这其实就是对图进行广度优先搜索的时间复杂度。
- **空间复杂度**$O(n + m)$：将输入转换成字典的空间复杂度为 $O(m)$。在广度优先搜索的过程中，我们需要最多 $O(n)$的队列空间进行广度优先搜索，以及若干个 $O(n)$的空间存储节点入度。



## 方法二：深度优先遍历（出度表）

### 算法流程

借助一个标志列表`flags`，用于判断每个节点`i`（课程）的状态：

- 未被`DFS`访问：`i == 0`；
- 已被**其他节点启动**的`DFS`访问：`i == -1`；
- 已被**当前节点启动**的`DFS`访问：`i == 1`。

对`numCourses`个节点依次执行`DFS`，判断每个节点起步`DFS`是否存在环，若存在环直接返回`False`。`DFS`流程：

- 终止条件：
  - 当`flag[i] == -1`，说明当前访问节点已被其他节点启动的`DFS`访问，无需再重复搜索，直接返回`True`。
  - 当`flag[i] == 1`，说明在本轮`DFS`搜索中节点`i`被第2次访问，即**课程安排图有环**，直接返回`False`。
- 将当前访问节点`i`对应`flag[i]`置`1`，即标记其被本轮`DFS`访问过；
- 递归访问当前节点`i`的所有邻接节点`j`，当发现环直接返回`False`；
- 当前节点所有邻接节点已被遍历，并没有发现环，则将当前节点`flag`置为`−1`并返回`True`。

若整个图`DFS`结束并未发现环，返回`True`。

### 代码

```c++
class Solution {
public:
    bool canFinish(int numCourses, std::vector<std::vector<int>>& prerequisites) {
        // 构建邻接表
        std::vector<std::vector<int>> adjacency(numCourses);
        for (const auto& cp : prerequisites) {
            adjacency[cp[1]].push_back(cp[0]);
        }

        // flags: 0 = 未访问, 1 = 正在访问（当前DFS路径上）, -1 = 已访问且无环
        std::vector<int> flags(numCourses, 0);

        for (int i = 0; i < numCourses; ++i) {
            if (!dfs(adjacency, flags, i)) {
                return false;
            }
        }
        return true;
    }

private:
    bool dfs(const std::vector<std::vector<int>>& adjacency,
             std::vector<int>& flags,
             int i) {
        if (flags[i] == 1) return false;   // 发现环
        if (flags[i] == -1) return true;   // 已确认无环

        flags[i] = 1;                      // 标记为正在访问
        for (int j : adjacency[i]) {
            if (!dfs(adjacency, flags, j)) {
                return false;
            }
        }
        flags[i] = -1;                     // 标记为已完成
        return true;
    }
};
```

### 复杂度分析

- **时间复杂度**$O(n+m)$：其中`n`为课程数，`m`为先修课程的要求数。这其实就是对图进行深度优先搜索的时间复杂度。
- **空间复杂度**$O(n+m)$：题目中是以列表形式给出的先修课程关系，为了对图进行深度优先搜索，我们需要存储成邻接表的形式，空间复杂度为 $O(n+m)$。在深度优先搜索的过程中，我们需要最多 $O(n)$的栈空间（递归）进行深度优先搜索，因此总空间复杂度为 $O(n+m)$。