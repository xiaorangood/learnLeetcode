# 题目描述

这是LeetCode上的[169.多数元素](https://leetcode.cn/problems/majority-element/)。

难度：**简单**

Tag：数组、哈希表、分治、计数、排序

给定一个大小为`n`的数组`nums`，返回其中的多数元素。多数元素是指在数组中出现次数**大于**`⌊n/2⌋`的元素。

你可以假设数组是非空的，并且给定的数组总是存在多数元素。

示例1：

```
输入：nums = [3,2,3]
输出：3
```


示例2：

```
输入：nums = [2,2,1,1,1,2,2]
输出：2
```

提示：

- `n == nums.length`
- `1 <= n <= 5 * 10000`
- `-10^9 <= nums[i] <= 10^9`
- 输入保证数组中一定有一个多数元素。

# 解题思路

本题常见的三种解法：

- **哈希表统计法**：遍历数组`nums`，用HashMap统计各数字的数量，即可找出众数。此方法时间和空间复杂度均为 $O(N)$。
- **数组排序法**：将数组`nums`排序，**数组中点的元素**一定为众数。
- **摩尔投票法**：核心理念为**票数正负抵消**。通过不断抵消不同的元素，最终留下的候选一定是多数元素（如果存在）。此方法时间和空间复杂度分别为 $O(N)$和 $O(1)$，为本题的最佳解法。

## 摩尔投票

设输入数组`nums`的众数为`x`，数组长度为`n`。

**推论一**：若记**众数**的票数为`+1`，**非众数**的票数为`−1`，则一定有所有数字的**票数和>0**。

**推论二**：若数组的前`a`个数字的**票数和=0**，则数组剩余`(n−a)`个数字的**票数和一定仍>0**，即后(n−a)个数字的**众数仍为`x`**。

根据以上推论，记数组首个元素为`n1`，众数为`x`，遍历并统计票数。当发生**票数和=0**时，**剩余数组的众数一定不变**，这是由于：

- **当`n1 == x`时**：抵消的所有数字中，有一半是众数`x`。
- **当`n1 != x`时**：抵消的所有数字中，众数`x`的数量最少为0个，最多为一半。

利用此特性，每轮假设发生**票数和=0**都可以**缩小剩余数组区间**。当遍历完成时，最后一轮假设的数字即为众数。

## 算法流程

- **初始化**：票数统计`votes=0`，众数`x`。
- **循环**：遍历数组`nums`中的每个数字`num`。
  - 当票数`votes`等于0，则假设当前数字`num`是众数。
  - 当`num == x`时，票数`votes`自增`1`；当`num != x`时，票数`votes`自减`1`。
- **返回值**：返回`x`即可。

## 示例

```
initial： votes = 0; x = 0
    [1,2,3,2,2,2,5,4,2]
1st:
    [1,2,3,2,2,2,5,4,2]
     ^
    num = 1;
    x = 1;      // votes == 0
    votes = 1;  // num == x
2nd:
    [1,2,3,2,2,2,5,4,2]
       ^
    num = 2;
    votes = 0;  // num != x
3rd:
    [1,2,3,2,2,2,5,4,2]
         ^
    num = 3;
    x = 3;      // votes == 0
    votes = 1;  // num == x
4th:[1,2,3,2,2,2,5,4,2]
           ^
    num = 2;
    votes = 0;  // num != x
5th:[1,2,3,2,2,2,5,4,2]
             ^
    num = 2;
    x = 2       // votes == 0
    votes = 1;  // num == x
6th:[1,2,3,2,2,2,5,4,2]
               ^
    num = 2;
    votes = 2;  // num == x
7th:[1,2,3,2,2,2,5,4,2]
                 ^
    num = 5;
    votes = 1;  // num != x
8th:[1,2,3,2,2,2,5,4,2]
                   ^
    num = 4;
    votes = 0;  // num != x
9th:[1,2,3,2,2,2,5,4,2]
                     ^
    num = 2;
    x = 2       // votes == 0
    votes = 1;  // num == x
```

## 代码

```c++
class Solution {
public:
    /**
     * 寻找数组中的多数元素（出现次数超过 ⌊ n/2 ⌋ 的元素）
     * 
     * 使用摩尔投票算法（Boyer-Moore Voting Algorithm）。
     * 核心思想：不同元素相互抵消，最后剩下的候选者即为多数元素。
     * 
     * @param nums 输入数组
     * @return 多数元素的值
     */
    int majorityElement(vector<int>& nums) {
        int x = 0;      // 当前候选的多数元素
        int votes = 0;  // 当前候选元素的净票数（优势票数）

        for (int num : nums) {
            // 如果当前净票数为0，说明之前的候选已被完全抵消，
            // 将当前元素设为新的候选
            if (votes == 0) {
                x = num;
            }
            // 如果当前元素等于候选，则净票数+1，否则-1
            votes += (num == x) ? 1 : -1;
        }

        // 由于题目保证多数元素一定存在，直接返回候选即可
        return x;
    }
};
```

## 复杂度分析

- **时间复杂度**$O(N)$：`N`为数组`nums`长度。
- **空间复杂度**$O(1)$：`votes`变量使用常数大小的额外空间。

