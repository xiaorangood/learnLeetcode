# 题目描述

这是LeetCode上的[300.最长递增子序列](https://leetcode.cn/problems/longest-increasing-subsequence/)。

难度：**中等**

Tag：xxxx

给你一个整数数组`nums`，找到其中最长严格递增子序列的长度。

**子序列**是由数组派生而来的序列，删除（或不删除）数组中的元素而不改变其余元素的顺序。例如，`[3, 6, 2, 7]`是数组`[0, 3, 1, 6, 2, 2, 7]`的子序列。

示例1：

```
输入：nums = [10,9,2,5,3,7,101,18]
输出：4
解释：最长递增子序列是 [2,3,7,101]，因此长度为 4 
```


示例2：

```
输入：nums = [0,1,0,3,2,3]
输出：4
```

示例3：

```
输入：nums = [7,7,7,7,7,7,7]
输出：1
```

提示：

- `1 <= nums.length <= 2500`
- `-10^4 <= nums[i] <= 10^4`

# 解题思路

## 解法一：动态规划

**状态定义**：`dp[i]`的值代表`nums`以`nums[i]`结尾的最长子序列长度。

**转移方程：**设`j∈[0, i)`，考虑每轮计算新`dp[i]`时，遍历`[0, i)`列表区间，做以下判断：

- 当`nums[i] > nums[j]`时：`nums[i]`可以接在`nums[j]`之后（此题要求严格递增），此情况下最长上升子序列长度为`dp[j] + 1`；
- 当`nums[i] <= nums[j]`时：`nums[i]`无法接在`nums[j]`之后，此情况上升子序列不成立，跳过。
- 上述所有第一种情况下计算出的`dp[j] + 1`的最大值，为直到`i`的最长上升子序列长度（即`dp[i]`）。实现方式为遍历`j`时，每轮执行`dp[i] = max(dp[i],dp[j] + 1)`。
- **转移方程**：`dp[i] = max(dp[i], dp[j] + 1) for j in [0, i)`。

**初始状态**：`dp[i]`所有元素置`1`，含义是每个元素都至少可以单独成为子序列，此时长度都为`1`。

**返回值**：返回`dp`列表最大值，即可得到全局最长上升子序列长度。

### 例子

```
初始状态
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 1, 1, 1, 1,  1]
i = 0, j = 0
    not goto 2nd loop, no operation
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 1, 1, 1, 1,  1]
i = 1, j = 0
    10 > 9, no operation
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 1, 1, 1, 1,  1]
i = 2, j = 0, 1
    10 and 9 both > 2, no operation
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 1, 1, 1, 1,  1]
i = 3, j = 0, 1, 2
    10 and 9 both > 5, no operation 
    2 < 5, dp[3] = max(dp[3], dp[2] + 1) = 2
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 2, 1, 1, 1,  1]
i = 4, j = 0, 1, 2, 3
    10 and 9 both > 3, no operation 
    2 < 3, dp[4] = max(dp[4], dp[2] + 1) = 2
    5 > 2, no operation
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 2, 2, 1, 1,  1]
i = 5, j = 0, 1, 2, 3, 4
    10 and 9 both > 7, no operation 
    2 < 7, dp[5] = max(dp[5], dp[2] + 1) = 2
    5 < 7, dp[5] = max(dp[5], dp[3] + 1) = 3
    3 < 7, dp[5] = max(dp[5], dp[4] + 1) = 3
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 2, 2, 3, 1,  1]
i = 6, j = 0, 1, 2, 3, 4, 5
    10 < 21, dp[6] = max(dp[6], dp[0] + 1) = 2
    9  < 21, dp[6] = max(dp[6], dp[1] + 1) = 2
    2  < 21, dp[6] = max(dp[6], dp[2] + 1) = 2
    5  < 21, dp[6] = max(dp[6], dp[3] + 1) = 3
    3  < 21, dp[6] = max(dp[6], dp[4] + 1) = 3
    7  < 21, dp[6] = max(dp[6], dp[5] + 1) = 4
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 2, 2, 3, 4,  1]
i = 7, j = 0, 1, 2, 3, 4, 5, 6
    10 < 18, dp[7] = max(dp[7], dp[0] + 1) = 2
    9  < 18, dp[7] = max(dp[7], dp[1] + 1) = 2
    2  < 18, dp[7] = max(dp[7], dp[2] + 1) = 2
    5  < 18, dp[7] = max(dp[7], dp[3] + 1) = 3
    3  < 18, dp[7] = max(dp[7], dp[4] + 1) = 3
    7  < 18, dp[7] = max(dp[7], dp[5] + 1) = 4
    21 > 18, no operation
    nums = [10, 9, 2, 5, 3, 7, 21, 18]
    dp   = [1,  1, 1, 2, 2, 3, 4,  4]
```

### 代码

```c++
class Solution {
public:
    int lengthOfLIS(vector<int>& nums) {
        int n = nums.size();
        if (n == 0) return 0;

        // dp[i] 表示以 nums[i] 结尾的最长递增子序列的长度
        vector<int> dp(n, 1);   // 每个元素自身构成长度为1的子序列
        int res = 0;

        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < i; ++j) {
                // 如果 nums[j] < nums[i]，则可以接在后面形成更长的递增子序列
                if (nums[j] < nums[i]) {
                    dp[i] = max(dp[i], dp[j] + 1);
                }
            }
            // 更新全局最大值
            res = max(res, dp[i]);
        }
        return res;
    }
};
```

### 复杂度分析

- **时间复杂度**$O(N^2)$：遍历计算`dp`列表需 $O(N)$，计算每个`dp[i]`需 $O(N)$。
- **空间复杂度**$O(N)$：`dp`列表占用线性大小额外空间。



## 解法二：动态规划+二分查找

**低复杂度切入点**：解法一中，遍历计算`dp`列表需 $O(N)$，计算每个`dp[k]`需 $O(N)$。

- 动态规划中，通过线性遍历来计算`dp`的复杂度无法降低；
- 每轮计算中，需要通过线性遍历`[0, k)`区间元素来得到`dp[k]`。
- 我们考虑：是否可以通过重新设计**状态定义**，使整个`dp`为一个**排序列表**；这样在计算每个`dp[k]`时，就可以通过二分法遍历`[0, k)`区间元素，将此部分复杂度由 $O(N)$降至 $O(\log N)$。

### 设计思路

**新的状态定义**：

- 我们考虑维护一个列表`tails`，其中每个元素`tails[k]`的值代表**在所有长度为`k+1`的递增子序列中，最小的末尾元素值**。
- 如`[1, 4, 6]`序列，长度为`1`，`2`，`3`的子序列尾部元素值分别为`tails = [1, 4, 6]`。
  - 长度为`1`的子序列：`[1]`，`[4]`，`[6]`；尾部元素分别是`1`，`4`，`6`。**最小的尾部是`1`**。
  - 长度为`2`的子序列：`[1,4]`，`[1,6]`，`[4,6]`；尾部元素分别是`4`，`6`，`6`。**最小的尾部是`4`**。
  - 长度为`3`的子序列：`[1,4,6]`；尾部元素是`6`。**最小的尾部是`6`**。

**状态转移设计**：

- 设常量数字`N`，和随机数字`x`，我们可以容易推出：当`N`越小时，`N < x`的几率越大。
- 例如：`N = 0`肯定比`N = 1000`更可能满足`N < x`。
- 在遍历计算每个`tails[k]`，不断更新长度为`[1, k]`的子序列尾部元素值，**始终保持每个尾部元素值最小**。
- **对于每个可能的子序列长度，只保留最小的那个尾部元素**，因为尾部越小，将来越容易接上更大的数字，从而有机会形成更长的递增子序列。
- 例如`[1, 5, 3]`，遍历到元素`5`时，长度为`2`的子序列尾部元素值为`5`；当遍历到元素`3`时，长度为`2`的子序列尾部元素值应更新至`3`，因为`3`遇到比它大的数字的几率更大。

`tails`列表一定是**严格递增**的，即当尽可能使每个子序列尾部元素值最小的前提下，子序列越长，其序列尾部元素值一定更大。**反证法证明**：

- 假设：`tails`不是严格递增，即
  - 当`k < i`，若`tails[k] >= tails[i]`
  - 即，较短子序列的尾部元素的值**大于**较长子序列的尾部元素的值。
- `tails[i]`是某个长度为`i + 1`的递增子序列的末尾，设这个子序列为 $S = [a_1,a_2,\dots ,a_i,tails[i]]$（序列严格递增）。
- 从长度为`i + 1`的子序列尾部倒序删除`i − k`个元素，剩下的为长度为`k + 1`的子序列。
- 得到一个新的子序列 $S' = [a_1, ..., a_k, a_{k+1}]$，其中 $a_{k+1}$就是原序列的第 `k + 1`个元素。
- 设此序列尾部元素值为`v`，则一定有`v < tails[i]`（即长度为`k`的子序列尾部元素值一定更小），这和`tails[k] >= tails[i]`矛盾。

既然严格递增，每轮计算`tails[k]`时就可以使用二分法查找需要更新的尾部元素值的对应索引`i`。

### 算法流程

**状态定义**：`tails[k]`的值代表长度为`k + 1`子序列的尾部元素值最小值。

**转移方程**：

- 设`res`为`tails`当前长度，代表直到当前的最长上升子序列长度。
- 设`j∈[0, res)`，考虑每轮遍历`nums[k]`时，通过二分法遍历`[0, res)`列表区间，找出`nums[k]`的大小分界点，会出现两种情况：
  - **区间中存在**`tails[i] > nums[k]`：将第一个满足`tails[i] > nums[k]`执行`tails[i] = nums[k]`；因为更小的`nums[k]`后更可能接一个比它大的数字（前面分析过）。
  - **区间中不存在**`tails[i] > nums[k]`：意味着`nums[k]`可以接在前面所有长度的子序列之后，因此肯定是接到最长的后面（长度为`res`），新子序列长度为`res + 1`。


**初始状态**：令`tails`列表所有值为`0`。

**返回值**：返回`res`，即最长递增子序列长度。

### 代码

```c++
class Solution {
public:
    int lengthOfLIS(vector<int>& nums) {
        int n = nums.size();
        // tails[k] 表示长度为 k+1 的递增子序列的最小末尾元素
        vector<int> tails(n);
        int res = 0;  // 当前 LIS 长度（tails 中有效元素个数）

        for (int num : nums) {
            // 二分查找第一个 >= num 的位置
            int i = 0, j = res;
            while (i < j) {
                int m = i + (j - i) / 2;
                if (tails[m] < num) {
                    i = m + 1;      // num 比中间值大，向右搜索
                } else {
                    j = m;          // num 小于等于中间值，向左搜索
                }
            }
            // 将 num 放到位置 i（替换或追加）
            tails[i] = num;
            // 如果 i 等于当前长度，说明 num 比所有尾部都大，LIS 长度增加
            if (i == res) {
                ++res;
            }
        }
        return res;
    }
};
```

### 复杂度分析

- **时间复杂度**$O(N \log N)$：遍历`nums`列表需 $O(N)$，在每个`nums[i]`二分法需 $O(logN)$。
- **空间复杂度**$O(N)$：`tails`列表占用线性大小额外空间。